spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:D:/leave-management/leave-management/config-repo

server:
  port: 8888
